listeners:
  - http://0.0.0.0:8080

workers:
  command: php artisan octane:start --server=frankenphp --host=0.0.0.0 --port=8080 --no-interaction
  reload:
    - .env
    - composer.lock
    - routes/
    - app/
    - config/
    - resources/
    - public/